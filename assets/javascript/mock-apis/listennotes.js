// listennotes requires a credit card, even for the free key...
// exceeding the call limit will incur charges,
// so it is dangerous to expose in the front-end

// what about a prepaid visa?
// otherwise use a prompt to enter keys before the app will function